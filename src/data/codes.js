// Redeem codes — typed at the prompt opened by `R`. Each code is
// single-use per session (tracked in registry.redeemedCodes).

export const CODES = {
  '1234': { gold: 50,    label: '+50 gold' },
  '2235': { gold: 250,   label: '+250 gold' }
};
